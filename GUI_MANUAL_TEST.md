# Framescreen2PDF v1.0.0 手动验收清单

## 启动

- [ ] 双击 `Framescreen2PDF.exe` 能启动服务。
- [ ] 浏览器自动打开 `http://127.0.0.1:18766/`。
- [ ] 首页显示 `Framescreen2PDF`。
- [ ] 页面显示“所有处理仅在本地完成”。

## Full 版

- [ ] 标识显示 Full。
- [ ] 图片、长截图、视频三个入口可见。
- [ ] Word 按钮在生成 PDF 后可用。
- [ ] 水印默认不勾选。
- [ ] 勾选水印并填写文字后，PDF 中出现对应水印。

## Lite 版

- [ ] 标识显示 Lite。
- [ ] Word 相关按钮和下载入口不可见。
- [ ] 水印选项不可编辑。
- [ ] 生成 PDF 后默认带 `Framescreen2PDF` 水印。

## 图片

- [ ] 可选择多张图片。
- [ ] 缩略图可预览。
- [ ] 可删除缩略图。
- [ ] 可拖拽排序。
- [ ] PDF 生成进度条会推进。
- [ ] PDF 下载文件名默认使用第一张图片名。
- [ ] Full 版可生成 Word，图片自适应页面并带编号。

## 长截图

- [ ] 可选择长截图。
- [ ] 可切片并生成缩略图。
- [ ] 可删除和拖拽排序切片。
- [ ] 可生成 PDF。
- [ ] Full 版可生成 Word。

## 视频

- [ ] 未安装 FFmpeg 时给出清晰提示。
- [ ] 安装 FFmpeg 后可选择视频。
- [ ] 抽帧会包含首帧。
- [ ] 处理过程中显示进度。
- [ ] 图像去重默认关闭。
- [ ] 用户手动开启图像去重后才进行去重。
- [ ] 框选主要区域后，筛选和预览使用该区域。
- [ ] 生成 PDF 前误点“选择下一个视频”会弹窗提醒。

## 任务历史恢复

- [ ] 生成 PDF 后刷新页面，「任务历史」面板仍显示该任务并可下载 PDF。
- [ ] 生成 PDF/Word 后关闭浏览器，重新打开 `http://127.0.0.1:18766/` 仍可下载。
- [ ] 关闭程序（黑框）后重新启动，历史任务仍可下载（1 天内）。
- [ ] 任务处理中关闭浏览器，后端继续处理；重新打开页面后状态自动更新为完成并可下载。
- [ ] 程序中断且未生成 PDF 的任务显示「已中断」。
- [ ] 视频/长截图任务处理完成（状态「完成」）但 PDF 未生成时，历史面板显示「生成 PDF」按钮，点击后可下载。
- [ ] Full 版可在历史面板补生成 Word / 图片ZIP。

## 可选附件

- [ ] `FFmpeg-video-component-installer.zip` 只包含说明和安装脚本，不包含 `ffmpeg.exe`。
- [ ] `PaddleOCR-PaddlePaddle-optional-installer.zip` 只包含说明和安装脚本，不包含 PaddleOCR/PaddlePaddle 本体。
